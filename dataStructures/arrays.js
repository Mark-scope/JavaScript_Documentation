let students = ['Ali', 'Omar', 90, true];
console.log(students);

students.pop(); // removes the last element
console.log(students);

students.push('Ahmed');
console.log(students);  // adds the last element

students.unshift('Ahmed'); // adds the first element
console.log(students);

students.shift(); // removes the first element
console.log(students)